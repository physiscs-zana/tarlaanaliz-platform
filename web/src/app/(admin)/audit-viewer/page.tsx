/* BOUND: TARLAANALIZ_SSOT_v1_2_0.txt – canonical rules are referenced, not duplicated. */
/* Bu sayfa /audit ile birlestirildi. */

import { redirect } from "next/navigation";

export default function AdminAuditViewerPage() {
  redirect("/audit");
}
